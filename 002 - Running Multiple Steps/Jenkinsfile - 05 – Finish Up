pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('build') {
            steps {
                echo "$( RANDOM % 2 )"
            }
        }
    }
}